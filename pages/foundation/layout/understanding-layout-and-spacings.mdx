import Divider from "@sonnat/ui/Divider";
import Row from "@sonnat/ui/Row";
import Column from "@sonnat/ui/Column";
import Code from "@sonnat/ui/Code";
import Image from "components/Image";
import Behaviour from "components/Behaviour";
import AnchorButton from "components/AnchorButton";
import WithTableOfContent from "components/layouts/WithTableOfContent";
import Head from "next/head";
import { defaultKeywordsMetaContent, siteFullAddress } from "sharedVars";
import {
  setCanonicalMeta,
  setDescriptionMeta,
  setKeywordsMeta,
  setTitleMeta
} from "utils";

export const meta = {
  title: "درک چیدمان و فاصله‌گذاری",
  description:
    "چیدمان و فاصله‌گذاری در همنشینی با یکدیگر ظاهر بصری، اولویت‌ها و وابستگی‌های المان‌های رابط کاربر را شفاف می‌کنند و هر کدام در دیزاین سیستم سنّت قواعد مشخصی دارند. با رعایت این قواعد، صفحات مختلف محصول ساختاری یکپارچه و باثبات خواهد داشت.",
  getLayout: () => page =>
    (
      <WithTableOfContent
        tableData={[
          { id: "معرفی", label: "معرفی" },
          { id: "چیدمان", label: "چیدمان" },
          { id: "فاصله‌گذاری", label: "فاصله‌گذاری" }
        ]}
      >
        {page}
      </WithTableOfContent>
    )
};

<Head>
  {setTitleMeta(`${meta.title} | دیزاین سیستم سنّت`)}
  {setDescriptionMeta(meta.description)}
  {setCanonicalMeta(
    `${siteFullAddress}/foundation/layout/understanding-layout-and-spacings`
  )}
  {setKeywordsMeta([
    ...defaultKeywordsMetaContent,
    "layout",
    "spacing",
    "spacings",
    "فاصله‌گذاری",
    "چیدمان"
  ])}
</Head>

<h1>
  <span>{meta.title}</span>
  <AnchorButton anchorId="معرفی" />
</h1>

<p>{meta.description}</p>

<h2>
  <span>چیدمان (Layout)</span>
  <AnchorButton anchorId="چیدمان" />
</h2>

<p>
  چیدمان جزء اصلی صفحهٔ وب واکنش‌گرا است که از تعدادی نگهدارنده تشکیل شده است تا
  فضای مجاز طراحی را تعیین کنند. چیدمان واکنش‌گرای نگه‌دارنده‌ها از رفتار گرید
  در صفحات مختلف پیروی می‌کند.
</p>

<Image
  imageProps={{
    src: "/static/media/layout-demo.svg",
    alt: "چیدمان (Layout)",
    unoptimized: true,
    width: 860,
    height: 428
  }}
/>

<h2>
  <span>فاصله‌گذاری (Spacing)</span>
  <AnchorButton anchorId="فاصله‌گذاری" />
</h2>

<p>
  فاصله‌گذاری در سنّت براساس واحدی ثابت انجام می‌شود. این واحد ثابت مضربی از عدد
  ۸ است و با توجه به ساختار و اجزای مختلف صفحه، از آن استفاده می‌شود. ۲ نوع
  فاصله‌گذاری در سنّت وجود دارد:
</p>

<ul>
  <li>
    <strong>فاصله‌گذاری بین اجزاء یک کامپوننت:</strong> حداقل و حداکثر فاصلهٔ هر
    جز در کامپوننت، 2dp و 16dp است.
  </li>
  <li>
    <strong>فاصله‌گذاری بین کامپوننت‌ها:</strong> حداقل و حداکثر فاصله‌ی
    کامپوننت‌ها از یکدیگر براساس صفحه و نوع چیدمان‌شان، 4dp و 128dp است.
  </li>
</ul>

<p>
  مقادیر تعیین‌شده در روش فاصله‌گذاری سنّت در فاصله‌ها (Paddings) و حاشیه‌های
  (Margins) المان‌ها و کامپوننت‌ها استفاده می‌شوند تا چیدمان‌ صفحات و اجزایشان
  باثبات و رفتار واکنش‌گرایشان پیش‌بینی‌پذیر باشد
</p>

<Image
  imageProps={{
    src: "/static/media/layout-spacings.svg",
    alt: "فاصله‌گذاری (Spacing)",
    unoptimized: true,
    width: 860,
    height: 396
  }}
/>
