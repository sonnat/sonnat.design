import Divider from "@sonnat/ui/Divider";
import Row from "@sonnat/ui/Row";
import Column from "@sonnat/ui/Column";
import Code from "@sonnat/ui/Code";
import Image from "components/Image";
import Behaviour from "components/Behaviour";
import Anatomy from "components/Anatomy";
import AnchorButton from "components/AnchorButton";
import WithTableOfContent from "components/layouts/WithTableOfContent";
import Head from "next/head";
import { defaultKeywordsMetaContent, siteFullAddress } from "sharedVars";
import {
  setCanonicalMeta,
  setDescriptionMeta,
  setKeywordsMeta,
  setTitleMeta
} from "utils";

export const meta = {
  title: "کنترل‌های‌ انتخاب",
  description:
    "کنترل‌های انتخابی امکان انتخاب از میان یک یا چند گزینهٔ ممکن را فراهم می‌کنند.",
  getLayout: () => page =>
    (
      <WithTableOfContent
        tableData={[
          { id: "معرفی", label: "معرفی" },
          { id: "کاربرد", label: "کاربرد" },
          { id: "انواع", label: "انواع" },
          { id: "آناتومی", label: "آناتومی" },
          { id: "رفتار", label: "رفتار" }
        ]}
      >
        {page}
      </WithTableOfContent>
    )
};

<Head>
  {setTitleMeta(`${meta.title} | دیزاین سیستم سنّت`)}
  {setDescriptionMeta(meta.description)}
  {setCanonicalMeta(`${siteFullAddress}/components/SwitchControls`)}
  {setKeywordsMeta([
    ...defaultKeywordsMetaContent,
    "switch",
    "checkbox",
    "دکمه رادیویی",
    "چک‌باکس‌",
    "کلید"
  ])}
</Head>

<h1>
  <span>{meta.title}</span>
  <AnchorButton anchorId="معرفی" />
</h1>

<p data-subtitle>
  دکمه‌‌ رادیویی (Radio button)، ‌چک‌باکس‌ (Checkbox) و کلید (Switch)
</p>

<p>{meta.description}</p>

<h2>
  <span>کاربرد</span>
  <AnchorButton anchorId="کاربرد" />
</h2>

<p>
  این کامپوننت‌ها انتخاب گزینه‌ها و یا خاموش و روشن کردن تنظیمات و تغییر
  وضعیت‌های موجود در محصول را میسر می کنند. مانند فعال یا غیرفعال‌کردن ویبره در
  صفحهٔ تنظیمات.
  <br />
  بهتر است کنترل‌های انتخاب همیشه لیبل متنی داشته باشند. لیبل متن شفاف و موثری
  است که کاربران را برای وارد کردن اطلاعات درست در فیلد متنی راهنمایی می‌کند.
  اگر کنترل‌های انتخاب لیبل متنی نداشته باشند باید رابطهٔ آنها با کامپوننت
  مربوطشان به‌وضوح مشخص باشد.
</p>

<Divider spaced />

<h2>
  <span>انواع</span>
  <AnchorButton anchorId="انواع" />
</h2>

<h3>
  <span>دکمهٔ رادیویی (Radio button)</span>
  <AnchorButton anchorId="دکمهٔ رادیویی (Radio button)" />
</h3>

<p>
  دکمه‌های رادیویی امکان انتخاب یک گزینه را از میان مجموعه‌ای از گزینه‌ها فراهم
  می‌کند. زمانی که لازم باشد کاربر تمام گزینه‌های موجود را ببیند اما فقط یکی از
  آن‌ها را انتخاب کند، از این کامپوننت استفاده می‌شود.
</p>

<Image
  caption="دکمهٔ رادیویی"
  imageProps={{
    src: "/static/media/switch-controls-radio.svg",
    alt: "دکمهٔ رادیویی",
    unoptimized: true,
    width: 667,
    height: 152
  }}
/>

<Divider spaced dotted />

<h3>
  <span>چک‌باکس (Checkbox)</span>
  <AnchorButton anchorId="چک‌باکس (Checkbox)" />
</h3>

<p>
  چک‌باکس‌ها امکان انتخاب یک یا چند گزینه را فراهم می‌کنند. همچنین از چک‌باکس‌ها
  برای روشن و خاموش کردن وضعیت‌ها در حالت دسکتاپ نیز استفاده می‌شود.
  <br />
  <strong>چک‌باکس‌های والد و فرزندی:</strong> چک‌باکس‌های والد و فرزندی زمانی
  استفاده می‌شوند که لیستی از گزینه‌های اصلی و فرعی وجود داشته باشد.
  <br />
  وقتی چک‌باکس‌ والد فعال شود همهٔ فرزند‌های آن نیز فعال می‌شوند و وقتی چک‌باکس
  والد غیرفعال باشد همهٔ فرزند‌های آن نیز غیرفعال می‌شوند. اگر بعضی از
  چک‌باکس‌های فرزندان فعال و بعضی از آن‌ها غیرفعال باشند، چک‌باکس‌ والد به حالت
  نامشخص (Indeterminate) تبدیل می‌شود.
</p>

<Image
  caption="چک‌باکس"
  imageProps={{
    src: "/static/media/switch-controls-checkbox.svg",
    alt: "چک‌باکس",
    unoptimized: true,
    width: 667,
    height: 272
  }}
/>

<Divider spaced dotted />

<h3>
  <span>کلید (Switch)</span>
  <AnchorButton anchorId="کلید (Switch)" />
</h3>

<p>
  کلیدها وضعیت یک المان را روشن یا خاموش می کنند. آنها بهترین روش برای مشخص کردن
  «مفهوم» تنظیمات درمحصول‌اند.
</p>

<Image
  caption="کلید"
  imageProps={{
    src: "/static/media/switch-controls-switch.svg",
    alt: "کلید",
    unoptimized: true,
    width: 667,
    height: 152
  }}
/>

<div>
  <p></p>
  <Divider spaced />
</div>

<h2>
  <span>آناتومی</span>
  <AnchorButton anchorId="آناتومی" />
</h2>

<p>
  اینجا می‌توانید ابعاد و مشخصه‌های دکمه‌های رادیویی، چک‌باکس‌ها و کلید‌ها را
  ببینید.
</p>

<div data-columnWrapper>
  <Row>
    <Column all="12" sm="4">
      <div data-column>
        <Anatomy
          caption="دکمهٔ رادیویی"
          imageProps={{
            mainSrc: "/static/media/switch-controls-anatomy-1.svg",
            specSrc: "/static/media/switch-controls-anatomy-1-spec.svg",
            alt: "دکمهٔ رادیویی",
            unoptimized: true,
            width: 200,
            height: 200
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="4">
      <div data-column>
        <Anatomy
          caption="چک‌باکس"
          imageProps={{
            mainSrc: "/static/media/switch-controls-anatomy-2.svg",
            specSrc: "/static/media/switch-controls-anatomy-2-spec.svg",
            alt: "چک‌باکس",
            unoptimized: true,
            width: 200,
            height: 200
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="4">
      <div data-column>
        <Anatomy
          caption="کلید"
          imageProps={{
            mainSrc: "/static/media/switch-controls-anatomy-3.svg",
            specSrc: "/static/media/switch-controls-anatomy-3-spec.svg",
            alt: "کلید",
            unoptimized: true,
            width: 200,
            height: 200
          }}
        />
      </div>
    </Column>
  </Row>
</div>

<div>
  <p></p>
  <Divider spaced />
</div>

<h2>
  <span>رفتار</span>
  <AnchorButton anchorId="رفتار" />
</h2>

<p>
  در راهنمای زیر رفتار کامپوننت در کاربردهای مختلف دیزاین توضیح داده شده است.
</p>

<div data-columnWrapper>
  <Row>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="از دکمه‌های رادیویی زمانی استفاده کنید که می‌خواهید همهٔ گزینه‌های ممکن را نمایش دهید ولی فقط یک گزینه انتخاب‌شدنی باشد."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-1.svg",
            alt: "از دکمه‌های رادیویی زمانی استفاده کنید که می‌خواهید همهٔ گزینه‌های ممکن را نمایش دهید ولی فقط یک گزینه انتخاب‌شدنی باشد.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="زمانی که می‌خواهید یک یا چند گزینه انتخاب شود، از چک‌باکس استفاده کنید."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-2.svg",
            alt: "زمانی که می‌خواهید یک یا چند گزینه انتخاب شود، از چک‌باکس استفاده کنید.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="dont"
          caption="اگر صفحه شامل انتخاب‌های مختلفی دربارهٔ یک موضوع مرتبط است که هرکدام می‌توانند فعال یا غیرفعال باشند، از کلید به‌جای چک‌باکس استفاده نکنید."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-3.svg",
            alt: "اگر صفحه شامل انتخاب‌های مختلفی دربارهٔ یک موضوع مرتبط است که هرکدام می‌توانند فعال یا غیرفعال باشند، از کلید به‌جای چک‌باکس استفاده نکنید.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="برای روشن یا خاموش‌کردن گزینه‌ای در موبایل یا تبلت از کلید استفاده می‌شود."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-4.svg",
            alt: "برای روشن یا خاموش‌کردن گزینه‌ای در موبایل یا تبلت از کلید استفاده می‌شود.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="برای فعال یا غیرفعال‌کردن آنی یک حالت از کلید استفاده کنید."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-5.svg",
            alt: "برای فعال یا غیرفعال‌کردن آنی یک حالت از کلید استفاده کنید.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="dont"
          caption="برای تغییر حالت یا تنظیمات به‌جای کلید از چک‌باکس استفاده نکنید."
          imageProps={{
            src: "/static/media/switch-controls-behaviour-6.svg",
            alt: "برای تغییر حالت یا تنظیمات به‌جای کلید از چک‌باکس استفاده نکنید.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
  </Row>
</div>
