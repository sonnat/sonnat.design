import Divider from "@sonnat/ui/Divider";
import Row from "@sonnat/ui/Row";
import Column from "@sonnat/ui/Column";
import Code from "@sonnat/ui/Code";
import Image from "components/Image";
import InputSliderDemo from "components/demos/InputSliderDemo";
import Behaviour from "components/Behaviour";
import Anatomy from "components/Anatomy";
import AnchorButton from "components/AnchorButton";
import WithTableOfContent from "components/layouts/WithTableOfContent";
import Head from "next/head";
import { defaultKeywordsMetaContent, siteFullAddress } from "sharedVars";
import {
  setCanonicalMeta,
  setDescriptionMeta,
  setKeywordsMeta,
  setTitleMeta
} from "utils";

export const meta = {
  title: "اسلایدر محدوده",
  description: "اسلایدر محدوده انتخاب مقدار یا بازه‌ای مشخص را فراهم می‌کند.",
  getLayout: () => page =>
    (
      <WithTableOfContent
        tableData={[
          { id: "معرفی", label: "معرفی" },
          { id: "دموی تعاملی", label: "دموی تعاملی" },
          { id: "کاربرد", label: "کاربرد" },
          { id: "انواع", label: "انواع" },
          { id: "آناتومی", label: "آناتومی" },
          { id: "رفتار", label: "رفتار" }
        ]}
      >
        {page}
      </WithTableOfContent>
    )
};

<Head>
  {setTitleMeta(`${meta.title} | دیزاین سیستم سنّت`)}
  {setDescriptionMeta(meta.description)}
  {setCanonicalMeta(`${siteFullAddress}/components/InputSlider`)}
  {setKeywordsMeta([
    ...defaultKeywordsMetaContent,
    "InputSlider",
    "اسلایدر محدوده"
  ])}
</Head>

<h1>
  <span>{meta.title}</span>
  <AnchorButton anchorId="معرفی" />
</h1>

<p data-subtitle>اسلایدر پیوسته (Continuous) و گسسته (Discrete)</p>

<p>{meta.description}</p>

<h2>
  <span>دموی تعاملی</span>
  <AnchorButton anchorId="دموی تعاملی" />
</h2>

<InputSliderDemo />

<h2>
  <span>کاربرد</span>
  <AnchorButton anchorId="کاربرد" />
</h2>

<p>
  نوار اسلایدر محدوده‌ای از مقادیر را مشخص می‌کند که می‌تواند انتخاب مقدار معین
  یا طیفی از آن محدوده را برای کاربر فراهم کند.
  <br />
  زمانی که انتخاب محدوده و یا مقدار نسبی از تعیین مقدار دقیق اهمیت بیشتری داشته
  باشد، از اسلایدر استفاده می‌شود. (به طور مثال در اسلایدر صدا، بلندی نسبی بیشتر
  از سطح دسیبل اهمیت دارد.)
  <br />
  در غیر این صورت، زمانی که لازم باشد کاربر مقدار دقیق عددی وارد کند، از فیلد
  متنی استفاده می‌شود. اسلایدرها برای تنظیماتی مانند صدا و روشنایی و یا برای
  اعمال فیلترهای عکس ایده‌آل‌اند.
  <br />
  تغییرات جابجایی اسلایدر در لحظه و بعد از کنش کاربر با کامپوننت اتفاق می‌افتد و
  همزمان با تغییر تنظیمات اسلایدر کاربر باید بتواند تاثیرات آن‌ را ببیند.
  <br />
  برای تعیین عدد مشخص از اسلایدر گسسته و در غیر این صورت از اسلایدر پیوسته
  استفاده می‌شود.
</p>

<h3>
  <span>اسلایدر یک‌جهته</span>
  <AnchorButton anchorId="اسلایدر یک‌جهته" />
</h3>

<p>برای کم یا زیاد کردن مقداری بیشینه از اسلایدر یک‌جهته استفاده می‌شود.</p>

<Image
  caption="اسلایدر یک‌جهته"
  imageProps={{
    src: "/static/media/input-slider.svg",
    alt: "اسلایدر یک‌جهته",
    unoptimized: true,
    width: 422,
    height: 148
  }}
/>

<Divider dotted />

<h3>
  <span>اسلایدر دو‌جهته</span>
  <AnchorButton anchorId="اسلایدر دو‌جهته" />
</h3>

<p>برای انتخاب مقداری کمینه یا بیشینه‌ از اسلایدر دو‌جهته استفاده می‌شود.</p>

<Image
  caption="اسلایدر دو‌جهته"
  imageProps={{
    src: "/static/media/input-slider-bidirectional.svg",
    alt: "اسلایدر دو‌جهته",
    unoptimized: true,
    width: 422,
    height: 148
  }}
/>

<Divider dotted />

<h3>
  <span>رفتار تعاملی اسلایدر دو‌جهته</span>
  <AnchorButton anchorId="رفتار تعاملی اسلایدر دو‌جهته" />
</h3>

<p>
  در اسلایدر دوجهته هرکدام از دسته‌های تعیین‌کنندهٔ مقدار بیشینه و کمینه، بازهٔ
  حرکتی دستهٔ دیگر را محدود می‌کنند. در محدودترین حالت ممکن بیشینه و کمینهٔ
  محدودهٔ تعیین‌شده برهم منطبق می‌شوند.
</p>

<Image
  imageProps={{
    src: "/static/media/input-slider-interaction.svg",
    alt: "رفتار تعاملی اسلایدر دو‌جهته",
    unoptimized: true,
    width: 714,
    height: 262
  }}
/>

<div>
  <p></p>
  <Divider spaced />
</div>

<h2>
  <span>انواع</span>
  <AnchorButton anchorId="انواع" />
</h2>

<h3>
  <span>اسلایدر پیوسته</span>
  <AnchorButton anchorId="اسلایدر پیوسته" />
</h3>

<p>این کامپوننت برای تنظیم و انتخاب مقداری نسبی از یک محدوده استفاده می‌شود.</p>

<Image
  caption="اسلایدر پیوسته"
  imageProps={{
    src: "/static/media/input-slider-continuous.svg",
    alt: "اسلایدر پیوسته",
    unoptimized: true,
    width: 422,
    height: 148
  }}
/>

<Divider spaced dotted />

<h3>
  <span>اسلایدر گسسته</span>
  <AnchorButton anchorId="اسلایدر گسسته" />
</h3>

<p>
  این کامپوننت برای تنظیم مقادیری مشخص (با ارجاع به نشانگر آن مقدار) مناسب‌اند.
  مقادیر مجاز و تعیین‌شدنی با علامت‌هایی روی نوار اسلایدر نشان داده می‌شوند و با
  انتخاب هر کدام از این مقادیر، دستهٔ اسلایدر به آن‌ می‌چسبد.
</p>

<Image
  caption="اسلایدر گسسته"
  imageProps={{
    src: "/static/media/input-slider-discrete.svg",
    alt: "اسلایدر گسسته",
    unoptimized: true,
    width: 422,
    height: 148
  }}
/>

<div>
  <p></p>
  <Divider spaced />
</div>

<h2>
  <span>آناتومی</span>
  <AnchorButton anchorId="آناتومی" />
</h2>

<p>اینجا می‌توانید ابعاد بخش‌های مختلف اسلایدرها را ببینید.</p>

<div data-columnWrapper>
  <Row>
    <Column all="12">
      <div data-column>
        <Anatomy
          caption="اسلایدر پیوسته"
          imageProps={{
            mainSrc: "/static/media/input-slider-anatomy-1.svg",
            specSrc: "/static/media/input-slider-anatomy-1-spec.svg",
            alt: "اسلایدر پیوسته",
            unoptimized: true,
            width: 520,
            height: 220
          }}
        />
      </div>
    </Column>
    <Column all="12">
      <div data-column>
        <Anatomy
          caption="اسلایدر گسسته"
          imageProps={{
            mainSrc: "/static/media/input-slider-anatomy-2.svg",
            specSrc: "/static/media/input-slider-anatomy-2-spec.svg",
            alt: "اسلایدر گسسته",
            unoptimized: true,
            width: 520,
            height: 220
          }}
        />
      </div>
    </Column>
  </Row>
</div>

<div>
  <p></p>
  <Divider spaced />
</div>

<h2>
  <span>رفتار</span>
  <AnchorButton anchorId="رفتار" />
</h2>

<p>
  در راهنمای زیر رفتار کامپوننت در کاربردهای مختلف دیزاین توضیح داده شده است.
</p>

<div data-columnWrapper>
  <Row>
    <Column all="12">
      <div data-column>
        <Behaviour
          variant="do"
          caption="تأثیرات تغییر مقادیر کمینه و بیشینهٔ اسلایدر باید محسوس باشد و در لحظه دیده شود. همچنین اسلایدرها باید وضعیت فعلی تنظیماتی را که کنترل می‌کنند، نمایش ‌دهند."
          imageProps={{
            src: "/static/media/input-slider-behaviour-1.svg",
            alt: "تأثیرات تغییر مقادیر کمینه و بیشینهٔ اسلایدر باید محسوس باشد و در لحظه دیده شود. همچنین اسلایدرها باید وضعیت فعلی تنظیماتی را که کنترل می‌کنند، نمایش ‌دهند.",
            unoptimized: true,
            width: 860,
            height: 375
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="برای دسترس‌پذیر بودن، اسلایدرها محدودهٔ کامل انتخاب‌ها را در دسترس کاربر قرار می‌دهند."
          imageProps={{
            src: "/static/media/input-slider-behaviour-2.svg",
            alt: "برای دسترس‌پذیر بودن، اسلایدرها محدودهٔ کامل انتخاب‌ها را در دسترس کاربر قرار می‌دهند.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="رنگ دستهٔ اسلایدر در حالت انتخاب‌شده ‌باید تغییر کند تا فعال‌بودن آن برای کاربر مشخص باشد."
          imageProps={{
            src: "/static/media/input-slider-behaviour-3.svg",
            alt: "رنگ دستهٔ اسلایدر در حالت انتخاب‌شده ‌باید تغییر کند تا فعال‌بودن آن برای کاربر مشخص باشد.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="do"
          caption="با کلیک روی نوار، مقدار اسلایدر به نقطهٔ کلیک تغییر می‌کند."
          imageProps={{
            src: "/static/media/input-slider-behaviour-4.svg",
            alt: "با کلیک روی نوار، مقدار اسلایدر به نقطهٔ کلیک تغییر می‌کند.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
    <Column all="12" sm="6">
      <div data-column>
        <Behaviour
          variant="warning"
          caption="کنش با اسلایدر برای محدوده‌های عددی خیلی کوچک (برای مثال بازهٔ ۱ تا ۳ ) و یا خیلی بزرگ (برای مثال بازهٔ ۱ تا ۱۰۰۰) غیر بهینه خواهد بود. برای این بازه‌ها دیگر کامپوننت‌های کنترل‌کننده جایگزین مناسب‌تری هستند."
          imageProps={{
            src: "/static/media/input-slider-behaviour-5.svg",
            alt: "کنش با اسلایدر برای محدوده‌های عددی خیلی کوچک (برای مثال بازهٔ ۱ تا ۳ ) و یا خیلی بزرگ (برای مثال بازهٔ ۱ تا ۱۰۰۰) غیر بهینه خواهد بود. برای این بازه‌ها دیگر کامپوننت‌های کنترل‌کننده جایگزین مناسب‌تری هستند.",
            unoptimized: true,
            width: 422,
            height: 360
          }}
        />
      </div>
    </Column>
  </Row>
</div>
